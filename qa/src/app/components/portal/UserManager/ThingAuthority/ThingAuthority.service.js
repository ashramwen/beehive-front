angular.module('BeehivePortal')
  .factory('ThingAuthorityService', ['$http', '$q', 'Session', function($http, $q, Session) {
    var thingAuthorityService = {};
    // TODO

    return thingAuthorityService;
  }]);
