'use strict';

angular.module('BeehivePortal')
  .factory('ScheduleTriggerDetailService',['$rootScope', '$$Type', '$q' , '$$Thing', function($rootScope, $$Type, $q, $$Thing) {
    var ScheduleTriggerDetailService = {};


    return ScheduleTriggerDetailService;
  }
]);