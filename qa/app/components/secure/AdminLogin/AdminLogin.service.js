angular.module('BeehivePortal')
  .factory('AdminLoginService', ['$http', '$q', 'Session', function($http, $q, Session) {
    var adminLoginService = {};
    // TODO

    return adminLoginService;
  }]);
