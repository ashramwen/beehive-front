angular.module('BeehivePortal')
  .factory('ThingDetailService', ['$http', '$q', 'Session', function($http, $q, Session) {
    var thingDetailService = {};
    // TODO

    return thingDetailService;
  }]);
