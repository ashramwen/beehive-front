angular.module('BeehivePortal')
  .factory('LocationViewService', ['$http', '$q', 'Session', function($http, $q, Session) {
    var locationViewService = {};
    // TODO

    return locationViewService;
  }]);
