angular.module('BeehivePortal')
  .factory('UserManagerService', ['$http', '$q', 'Session', function($http, $q, Session) {
    var userManagerService = {};
    // TODO

    return userManagerService;
  }]);
