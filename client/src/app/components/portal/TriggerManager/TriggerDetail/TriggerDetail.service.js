'use strict';

angular.module('BeehivePortal')
  .factory('TriggerDetailService',['$rootScope', '$$Type', '$q' , '$$Thing', function($rootScope, $$Type, $q, $$Thing) {
    var TriggerListService = {};

    return TriggerListService;
  }
]);