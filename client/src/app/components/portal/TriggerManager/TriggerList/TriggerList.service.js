'use strict';

angular.module('BeehivePortal')
  .factory('TriggerListService',['$rootScope', '$$Type', '$q' , '$$Thing', function($rootScope, $$Type, $q, $$Thing) {
    var TriggerListService = {};

    return TriggerListService;
  }
]);